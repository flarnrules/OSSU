(* this is a test file for exploring emacs *)

val test = 1;
val test_add = 1 + 1;
val test_add_variables = test + test_add;

(* to open the *sml* buffer where we can run `use` we need to run `C-c C-s` *)
(* we might need to save the file first, with `C-x C-s` *)
